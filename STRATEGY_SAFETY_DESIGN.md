# 多幣 Breakout 實盤執行器：策略與安全設計

**版本：** 2026-08-14
**狀態：** 僅供唯讀與模擬驗證；程式預設 `LIVE_TRADING=false`。

> 本文件將使用者指定的規則轉成可測試的程式行為，並不構成獲利保證或投資建議。任何實盤啟用都應在完整的唯讀、模擬與小額風險驗證後，由使用者自行決定。

## 1. 策略範圍與訊號

系統不再固定監控 `BTC_USDT_PERP`。每次合格掃描會先取得所有交易中的 USDT 永續合約，再以 24 小時成交額排序，取前 30 個候選標的。這避免把「漲跌幅最大」錯當成「熱門」，同時排除下架、非永續與非 USDT 計價合約。[1] [2]

每個候選標的只使用已完成的 K 線。Breakout 訊號採用 **20 期布林通道與 14 期 RSI 雙重確認**：收盤價突破上軌且 RSI 高於 55 時為 `LONG`；收盤價跌破下軌且 RSI 低於 45 時為 `SHORT`；其餘情況為 `HOLD`。此版先保持明確且可測試的門檻，不把「進行中 K 線」當成確認訊號。

| 項目 | 預設值 | 安全用途 |
| --- | ---: | --- |
| `SCAN_TOP_N` | 30 | 每輪僅分析成交額最高的前 30 個合格 USDT 永續合約。 |
| `KLINE_INTERVAL` | `5M` | 與既有實盤範本一致；僅使用最新一根之前的完成 K 線。 |
| `BOLLINGER_PERIOD` | 20 | 計算中軌與上下軌的收盤價期數。 |
| `BOLLINGER_STDDEV` | 2 | 布林通道標準差倍數。 |
| `RSI_PERIOD` | 14 | 相對強弱指標期數。 |
| `RSI_LONG_MIN` / `RSI_SHORT_MAX` | 55 / 45 | 多空雙向動能確認門檻。 |
| `SCAN_COOLDOWN_SECONDS` | 60 | 即使主迴圈每 10 秒執行，也不會每次都重抓 30 組 K 線。 |

## 2. 倉位階梯

倉位以「合約帳戶可用 USDT」為依據。階梯中的金額是 **保證金**，名目價值仍依 `保證金 × 槓桿` 計算；在本版預設 5 倍槓桿下，50 USDT 保證金對應約 250 USDT 名目額。交易對的最小名目額、最小數量與數量步進仍必須以派網即時交易對規格為準。[2]

| 可用 USDT | 單筆保證金 | 行為 |
| ---: | ---: | --- |
| < 50 | 0 | 禁止開倉。 |
| 50–119.99 | 50 | 第一階。 |
| 120–239.99 | 100 | 第二階。 |
| 240–479.99 | 200 | 第三階。 |
| 480–959.99 | 400 | 第四階。 |
| 960–1,919.99 | 800 | 第五階。 |
| 1,920–3,839.99 | 1,600 | 第六階。 |
| 3,840–7,679.99 | 3,200 | 第七階。 |
| ≥ 7,680 | 6,400 | 封頂；帳戶即使超過 10,000 USDT 仍不提高單筆保證金。 |

`MIN_FREE_BALANCE_BUFFER` 保留為可選設定，預設改為 `0`，才會精確滿足「可用餘額至少 50 USDT 即可進入第一階」的規則。若日後將其設為大於零，開倉時仍必須額外保留該緩衝金額。

## 3. 倉位與風控狀態機

本版採 **單一活動倉位**。系統首先讀取帳戶全部非零持倉；只要存在任一持倉，就只進行風控監測，不掃描或開啟新的策略倉位。這個限制避免多標的同時持倉使可用餘額、槓桿與每日限額的風險快速放大。派網官方帳戶端點在省略 `symbol` 時會回傳所有當前持倉。[3]

ROE 使用派網回傳的 `unrealizedPnL / initialMargin × 100` 計算。當持倉目前 ROE 達到 10% 時，立即啟動 5% 保護線；只要 ROE 回落至 5% 或以下即平倉。若系統曾觀察到 ROE 達到 15%，則記錄高水位旗標，日後 ROE 回落至 10% 或以下立即平倉。硬停損維持使用者指定的 -8% ROE。

| 觸發條件 | 行為 | 優先順序 |
| --- | --- | ---: |
| ROE ≤ -8% | 市價、`reduceOnly=true` 平倉。 | 1 |
| 曾達 ROE ≥ 15%，且目前 ROE ≤ 10% | 立即鎖利平倉。 | 2 |
| 目前 ROE ≥ 10% | 將保護底線設定為 +5%。 | 3 |
| 已啟動 +5% 保護線且 ROE ≤ 5% | 立即保護性平倉。 | 4 |
| 其他情況 | 只記錄監控資料。 | 5 |

因為 15% 高水位是歷史狀態，系統會依 `positionId` 儲存峰值與「曾達 15%」旗標。若狀態檔在重新部署時消失，程式不會假裝知道重啟前是否曾達 15%，因此可能失去第二段鎖利的歷史判斷。Render 免費方案沒有持久化磁碟，故在這個限制未解決前，**不可將此程式切換為實盤交易**；目前僅應執行 `LIVE_TRADING=false` 的唯讀驗證。

## 4. API 與下單安全邊界

候選清單採 `GET /api/v1/common/symbols?type=PERP&status=TRADING` 與 `GET /api/v1/market/tickers?type=PERP` 的交集，排序欄位是 ticker 的 `amount`。新訊號出現時才讀取該交易對的槓桿、交易規格與 bid/ask。單一交易對的槓桿查詢需要指定 `symbol`，所以掃描中的其他標的即使槓桿不同，也不會阻斷雷達。[1] [2] [3]

在 `LIVE_TRADING=false` 下，任何開倉或平倉指令都只能寫入 `DRY_RUN_ORDER` 日誌，絕不呼叫派網下單端點，也不可設定槓桿。唯讀階段的訊號、槓桿不一致、可用餘額不足與交易對規格錯誤都應完整記錄，供後續檢查。

每日交易上限維持 3 筆，但只在 **實際送出開倉指令** 時累加。這讓唯讀驗證可重複觀察所有訊號，而不會被模擬日誌虛耗額度。日後要啟用實盤前，還必須將每日計數與持倉高水位保存至可靠的持久化儲存，避免雲端重啟後失去風控狀態。

## 5. 驗證範圍

離線測試將覆蓋階梯邊界、數量步進、交易對清單交集與成交額排序、RSI／布林訊號，以及所有 ROE 風控分支。測試不載入 `.env`、不呼叫網路，也不會送出任何訂單。程式通過離線測試後，才進行一次 `LIVE_TRADING=false` 的 API 唯讀驗證。

## References

[1]: https://www.pionex.com/docs/api-docs/futures-api/market "Pionex Futures API — Market"
[2]: https://www.pionex.com/docs/api-docs/futures-api/common "Pionex Futures API — Common"
[3]: https://www.pionex.com/docs/api-docs/futures-api/account "Pionex Futures API — Account"
